enum ProductType{
  food,
  drink,
  desserts,
}
enum OrderStatus{
   pending,
   confirmed,
   completed,
   canceled,
}

enum PaymentStatus{
   pending,
   paid,
}

enum ReservationStatus{
  available,
  reserved,
  canceled,
}